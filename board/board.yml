spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://kitchadb.cnqc2iqcsmsn.us-west-1.rds.amazonaws.com:3306/kitcha?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Seoul
    username: ${MYSQL_USERNAME}
    password: ${MYSQL_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect

  cloud:
    aws:
      s3:
        region: ${AWS_REGION}
        bucket: ${AWS_S3_BUCKET_NAME}
      credentials:
        access-key: ${AWS_ACCESSKEY}
        secret-key: ${AWS_SECRETKEY}

eureka:
  instance:
    prefer-ip-address: false
    hostname: alb-kitcha-board-1015991638.us-west-1.elb.amazonaws.com
    non-secure-port: 80
    non-secure-port-enabled: true
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      default-zone: http://54.215.85.146:8761/eureka/

logging:
  level:
    root: DEBUG
    org.springframework: INFO
    com.yourpackage: DEBUG

